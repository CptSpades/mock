{
  "name": "mock",
  "version": "1.0.0",
  "description": "Mock Json Server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "server": "node_modules/.bin/json-server ./src/server.js"
  },
  "author": "Rui Rego",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "json-server": "^0.12.0"
  }
}
